{
  "name": "rolecraft-action",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub Action for rolecraft — install and verify AI agent skills in CI",
  "scripts": {
    "test": "node --test --test-concurrency=1"
  }
}
